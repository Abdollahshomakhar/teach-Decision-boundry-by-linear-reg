
# 🧠 Logistic Regression Decision Boundary Visualization

این پروژه یک مثال ساده از **رگرسیون لجستیک (Logistic Regression)** با استفاده از داده‌های دوبعدی است. در این مثال، یک مدل برای طبقه‌بندی نقاط دوبعدی آموزش داده می‌شود و سپس **مرز تصمیم (Decision Boundary)** مدل رسم می‌گردد.

---

## 📊 توضیح پروژه

مدل Logistic Regression یکی از الگوریتم‌های پایه‌ای یادگیری ماشین است که برای **طبقه‌بندی دودویی (Binary Classification)** استفاده می‌شود.  
در این پروژه:

- داده‌های ساده‌ی دوبعدی تعریف می‌شوند  
- مدل روی این داده‌ها آموزش می‌بیند  
- مرز تصمیم مدل (Decision Boundary) روی نمودار ترسیم می‌شود  
- در پایان، یک نمونه‌ی جدید تست می‌شود تا کلاس آن پیش‌بینی شود

---

## ⚙️ پیش‌نیازها

برای اجرای این کد، باید کتابخانه‌های زیر نصب باشند:

```bash
pip install numpy matplotlib scikit-learn
